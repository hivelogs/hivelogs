# TASK-03 — Application Setup

## Objetivo

`ISetupService`, validators, abstrações (`IPasswordHasher`, repositórios), testes Application.

## Critérios de aceite

- [ ] `SetupService` com `GetStatusAsync` e `InitializeAsync`
- [ ] Testes com doubles in-memory
