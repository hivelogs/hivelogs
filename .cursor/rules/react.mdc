---
description: Convenções React/TypeScript para o dashboard HiveLogs
globs: apps/web/**
alwaysApply: false
---

# HiveLogs — Web

## Stack

React, TypeScript, Vite, Tailwind CSS, shadcn/ui, TanStack Query, React Hook Form, Zod, ky, React Router.

## Segurança

- `VITE_*` apenas para URLs públicas (ex: `VITE_API_BASE_URL`)
- Nunca Backend Secret Key, MCP Access Token ou JWT hardcoded

## Responsabilidade

- Consumir API; exibir dados; formulários de gestão
- Sem lógica de ingestão (isso é `apps/api` + SDKs)

## Organização

- `src/app/` — shell, providers, router, estilos globais
- `src/features/<nome>/` — api, hooks, schemas, types, components por domínio de tela
- `src/pages/` — páginas finas por rota
- `src/shared/api/` — httpClient, ProblemDetails, ApiError, parseApiError
- `src/shared/ui/` — componentes shadcn/ui
- `src/shared/config/` — env
- Validação: Zod + RHF; servidor: TanStack Query

## Referências

- Guia: [docs/guides/web-dashboard-development.md](../../docs/guides/web-dashboard-development.md)
- Design: [docs/design.md](../../docs/design.md), [docs/guides/design-handoff-pencil.md](../../docs/guides/design-handoff-pencil.md)
- Skills: `hivelogs-web-dashboard`, `hivelogs-web-design-handoff`
